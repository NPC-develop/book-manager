spring:
  datasource:
    url: jdbc:mysql://localhost:3307/book_db?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password:

mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true

server:
  port: 8080